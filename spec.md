# Specification — {{SITE_NAME}}

> BINDING contract (design doc §7.1.6): no implementation before Gate G2 passes.
> Every heading below must be non-empty. "The spec doesn't say" ⇒ ask or amend
> the spec — never improvise silently. Formulas and data require citations.

## 1. Overview

<!-- What the tool does, for whom, and the single problem it solves. -->

## 2. Page inventory

<!-- Every route this site ships at launch: /, legal set, landing pages. -->

## 3. Features

<!-- Numbered feature list. Each demonstrable. F-01, F-02, … -->

## 4. Formulas & data sources (cited)

<!-- Every formula/constant with its authoritative source. -->

## 5. Dataset schema

<!-- If the tool needs data files: schema + per-datum `source` and `retrieved`
     fields. "None" if no dataset. -->

## 6. Keyword map

<!-- Main keyword, supporting keywords (from G1 artifacts), and which page
     targets which keyword. -->

## 7. Island/hydration plan

<!-- Which parts need client JS. Default: vanilla <script>; React island only
     if complexity demands (§7.2 decision tree). -->

## 8. A11y notes

<!-- Keyboard operation, ARIA roles, contrast considerations for the tool UI. -->

## 9. Out of scope

<!-- Explicitly excluded features (prevents scope invention during build). -->

## 10. Acceptance checklist

<!-- Per feature: 3 known input→output pairs used by G4 tests.
     - [ ] F-01: input X → output Y (commit ref: …) -->

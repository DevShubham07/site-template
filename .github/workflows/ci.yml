name: CI
on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build-test:
    # Reusable fleet CI — patched once in ci-templates, inherited everywhere.
    uses: DevShubham07/ci-templates/.github/workflows/astro-ci.yml@v1
    secrets: inherit

---
import Base from "../layouts/Base.astro";
import { SITE } from "../../site.config.mjs";
---

<Base
  title={`Page Not Found — ${SITE.name}`}
  description="The page you are looking for does not exist."
  noindex
>
  <div class="flex flex-col items-center py-16 text-center">
    <p class="text-6xl font-bold tracking-tight">404</p>
    <h1 class="mt-3 text-2xl font-semibold">Page not found</h1>
    <p class="mt-2 max-w-md text-(--color-fg-muted)">
      The page you're looking for doesn't exist or was moved.
    </p>
    <a
      href="/"
      class="mt-6 rounded-md bg-(--color-accent) px-4 py-2 text-sm font-medium text-(--color-accent-fg) hover:opacity-90"
    >
      Back to {SITE.name}
    </a>
  </div>
</Base>
